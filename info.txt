Validé el archivo pegado. Está casi bien, pero todavía le falta el ajuste principal para OS.
Ahora mismo tu enrich_page() hace esto:
Python
tipo = detect_tipo(...)
oc_num = extract_oc_from_text(...)
os_num = extract_os_from_text(...)
pero no corrige tipo cuando os_num existe. Por eso te pasa esto:
Plain text
tipo_detectado = otro
orden_servicio = 000223
clave_documental = NULL
El snippet actual muestra que orden_servicio sí se llena, pero tipo queda como venía de detect_tipo. �
Código pegado: py
Corrige solo enrich_page()
Reemplaza el inicio por esto:
Python

Y confirma que tengas esto:
Python
elif tipo == "orden_servicio":
    os_data = extract_os(text, cliente)
    data.update({
        "orden_servicio": os_data["numero"],
        "clave_documental": os_data["clave"],
    })
Resultado esperado
Después de reclasificar:
Plain text
04-0060 P2 -> orden_servicio clave=OS|000223
04-0194 P2 -> orden_servicio clave=OS|000221
Eso es lo que falta.