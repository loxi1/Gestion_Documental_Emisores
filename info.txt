Sí, estuvo bien quitar la duplicada, pero no debes eliminar completamente is_documento_extranjero_o_proforma() porque is_factura_text() todavía la usa. El primer error fue por eso. �
Pasted text.txt
Ahora ya mejoró bastante:
Plain text
01-0067 -> nota_credito OK
01-0069 -> nota_credito OK
01-0070 -> nota_credito OK
01-0083 -> factura OK
Lo pendiente real ahora son 29 casos, principalmente facturas F051 sin RUC y 2 OTRO.
Cambio clave para los F051 sin RUC
Esas facturas parecen tener el RUC emisor mal OCR o no lo lee, pero tienen serie/número. Mejor mándalas a revision_manual, no distribuir como SINRUC.
En clasificar_paginas.py, cambia esta parte:
Python
if data["tipo"] in ("factura", "nota_credito", "guia_remision"):
    if not data.get("serie") or not data.get("numero") or not data.get("ruc") or not data.get("clave_documental"):
        nuevo_estado = "revision_manual"
por esta:
Python
if data["tipo"] in ("factura", "nota_credito", "guia_remision"):
    if (
        not data.get("serie")
        or not data.get("numero")
        or not data.get("ruc")
        or data.get("ruc") == "SINRUC"
        or not data.get("clave_documental")
        or "|SINRUC|" in (data.get("clave_documental") or "")
    ):
        nuevo_estado = "revision_manual"
Así no pasa nada tributario con RUC incompleto.
Mejora para extract_factura_from_text()
Agrega fallback para capturar el RUC antes de la razón social del emisor si aparece cerca de FACTURA ELECTRONICA.
Reemplaza solo la parte de RUC por:
Python
ruc_val = None

ruc_patterns = [
    r"DATOS\s+DEL\s+EMISOR[\s\S]{0,300}?RUC\s*:?\s*((10|20)\d{9})",
    r"FACTURA\s+ELECTR[ÓO]NICA[\s\S]{0,120}?R\.?\s*U\.?\s*C\.?\s*(?:N[°º])?\s*:?\s*((10|20)\d{9})",
    r"R\.?\s*U\.?\s*C\.?\s*(?:N[°º])?\s*:?\s*((10|20)\d{9})",
    r"RUC\s+EMISOR\s*:?\s*((10|20)\d{9})",
]

for pattern in ruc_patterns:
    m = re.search(pattern, t, re.I)
    if m:
        ruc_val = m.group(1)
        break
Qué hacer ahora
No limpies. Corrige clasificar_paginas.py y vuelve a correr solo:
Bash
python app/clasificar_paginas.py --year 2026 --cliente CIMA --month 1
python app/reporte_revision_manual.py --year 2026 --cliente CIMA --month 1
Luego editas solo esos 29, o si confirmas que todas las F051 son del mismo emisor, podemos hacer una actualización masiva por serie F051.